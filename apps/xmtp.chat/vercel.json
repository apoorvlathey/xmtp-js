{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "buildCommand": "cd ../.. && yarn install && yarn workspace @xmtp/xmtp.chat build",
  "outputDirectory": "dist"
}
